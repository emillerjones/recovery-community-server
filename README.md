
npm run db:reset


Backend API for a modern community platform. Built with Node.js, Express, and PostgreSQL, it handles authentication, user management, and the REST API consumed by the React frontend.